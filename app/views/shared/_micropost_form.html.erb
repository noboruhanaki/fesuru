<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "フェスのレビューを記載ください..." %>
    <%= hidden_field_tag :fesu_id, @fesus.id %>
  </div>
  <%= f.submit "レビューを書く", class: "btn btn-primary" %>
<% end %>